# ============================================================
# PriceWise — 环境变量配置
# ============================================================
# 复制此文件为 .env 并根据实际情况修改
#   cp .env.example .env
# ============================================================

# MySQL
MYSQL_ROOT_PASSWORD=root123
MYSQL_PASSWORD=pricing_api_2026
MYSQL_PORT=3306

# API Server
API_PORT=8080
JWT_SECRET=SmartPricingEngine-JWT-SecretKey-2026-Must-Be-At-Least-256-Bits-Long!!
CORS_ORIGINS=http://localhost:8080,http://localhost:80,http://localhost

# Web (Nginx)
WEB_PORT=80
