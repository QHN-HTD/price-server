# CLAUDE.md — 项目操作规则

> 本文件为项目级别的 Claude Code 操作规则，在每次代码更改前必须遵守。  
> 项目：电商智能定价决策引擎 (PriceWise)  
> 创建日期：2026-06-01

---

## 🔴 核心规则（每次操作前必读）

### 规则 1：禁止直接删除文件

**永远不要直接删除任何项目文件。**

正确流程：
1. 先将要删除的文件重命名或移动到临时目录（如 `_to_delete/`）
2. 完成项目修改后，将结果展示给用户检查
3. 由用户确认后再执行最终删除

错误示例：
```
❌ rm file.java
❌ git rm file.java
❌ 直接覆盖写入可能产生多余的文件
```

正确示例：
```
✅ 先将旧文件重命名为 file.java.bak 或移入 _to_delete/ 目录
✅ 等待用户检查项目运行结果
✅ 用户确认后，再删除备份文件
```

### 规则 2：按框架逐步生成

**严格遵循设计文档中定义的模块框架，逐步生成代码。**

正确流程：
1. 确认当前处于哪个模块（参见 `docs/superpowers/specs/2026-06-01-smart-pricing-engine-design.md`）
2. 完成一个模块后再进入下一个模块
3. 每个模块完成后暂停，等待用户确认
4. 用户确认后才继续推进

执行顺序：
```
数据层(Spark ETL) → 服务层(Spring Boot) → 可视化层(ECharts)
     ↓                    ↓                       ↓
  先确认数据          再确认接口              最后确认页面
```

### 规则 3：框架变更必须协商

**如果认为设计文档中的框架需要调整，必须先和用户商量。**

正确流程：
1. 发现需要调整的地方 → 提出具体建议和理由
2. 用户同意后 → 先更新设计文档 `docs/superpowers/specs/2026-06-01-smart-pricing-engine-design.md`
3. 设计文档更新后 → 再按新框架实施代码

**绝不允许**在没有用户同意的情况下偏离设计文档。

---

## 📋 每次操作前的检查清单

在开始任何代码更改前，逐项确认：

- [ ] 我已读取 `docs/superpowers/specs/2026-06-01-smart-pricing-engine-design.md` 了解当前设计
- [ ] 我确认不需要直接删除任何文件（如需删除，先备份等用户确认）
- [ ] 我确认当前操作在既定框架内（如不在，先和用户商量）
- [ ] 我确认当前模块的上一个模块已完成并经过用户确认

---

## 📁 项目文档索引

| 文档 | 路径 | 用途 |
|------|------|------|
| 项目设计规格 | `docs/superpowers/specs/2026-06-01-smart-pricing-engine-design.md` | 完整项目设计，每次操作前必读 |
| 操作规则（本文件）| `CLAUDE.md` | 每次代码更改前必读 |

---

## ⚠️ 特别注意

- 本项目的目标是 **求职作品集**，代码质量、文档完整性、架构合理性同等重要
- 安全设计是项目的核心亮点之一，任何修改都不能削弱安全措施
- 数据脱敏逻辑在 Spark ETL 阶段就必须落实，不可省略
